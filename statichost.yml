domains:
  # Primary domain
  - eca11y.shop
  # Domains that redirect to the primary
  - www.eca11y.shop
image: node:18.17.1
command: rm -rf /dist && npm install && npx parcel build src/*.html
public: dist
